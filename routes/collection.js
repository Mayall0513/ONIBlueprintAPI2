const express = require('express');
const Collection = require('../schema/blueprint').collectionModel;

const router = express.Router();

router.get("/", (request, response) => {
});

router.post("/", (request, response) => {
});

module.exports = router;